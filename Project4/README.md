# MySQLite Core Java Project

## 📌 Project Overview
This is a **Core Java project** that uses **SQLite (MySQLite)** as a local database.  
The project demonstrates how to connect Java with SQLite using **JDBC** and perform **CRUD operations** without using any external server.

SQLite is a **file-based database**, so no installation or server setup is required.

---

## 🛠️ Technologies Used
- **Java (Core Java)**
- **SQLite (MySQLite)**
- **JDBC (SQLite JDBC Driver)**
- **IDE:** IntelliJ IDEA / Eclipse
- **JDK:** Java 8 or above

---

## 📂 Project Structure
